복습

모던스크립트 - 딥다이브
json, ajax기술을 지원하는 모던스킄립트가 promise, async, await

함수만들때 - 주의할점
1. 한번에 한가지 기능만
2. 입력과 출력을 명확하게 입력 - 매개변수, 출력 - 반환값
3. 함수안에서 if문 쓸때(오류처리할때) 가급적 if-else 쓰지말기

if(error1)
{
    //에러처리
    return;
}

if(error2)
{
    //에러처리
    return;
}

if(error3)
{
    //에러처리
    return;
}

정상처리




